{% extends 'base.html.twig' %}

{% block title %}{{ game.title }}{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>{{ game.title }}</h1>
        <div>
            <a href="{{ path('app_game_edit', {'id': game.id}) }}" class="btn btn-warning">Éditer</a>
            <a href="{{ path('app_enigma_index', {'gameId': game.id}) }}" class="btn btn-secondary">Gérer les énigmes</a>
            <a href="{{ path('app_game_index') }}" class="btn btn-secondary">Retour</a>
        </div>
    </div>

    {% for message in app.flashes('success') %}
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            {{ message }}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    {% endfor %}

    <div class="card">
        {% if game.welcomeImg %}
            <img src="{{ game.welcomeImg }}" class="card-img-top" alt="{{ game.title }}">
        {% endif %}
        <div class="card-body">
            <h5 class="card-title">Message de bienvenue</h5>
            <p class="card-text">{{ game.welcomeMsg }}</p>
            
            <hr>
            
            <h5>Énigmes</h5>
            <p>Ce jeu contient {{ game.enigmas|length }} énigme(s)</p>
            
            <div class="mt-3">
                <a href="{{ path('app_play_game_select', {'id': game.id}) }}" class="btn btn-success" target="_blank">
                    🎮 Tester le jeu
                </a>
            </div>
        </div>
    </div>

    <div class="mt-4">
        <form method="post" action="{{ path('app_game_delete', {'id': game.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce jeu ?');">
            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ game.id) }}">
            <button class="btn btn-danger">Supprimer le jeu</button>
        </form>
    </div>
</div>
{% endblock %}
