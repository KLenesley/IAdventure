{% extends 'base.html.twig' %}

{% block title %}Tableau de bord{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>Tableau de bord</h1>
    
    <div class="row mt-4">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    <h5>Mes Jeux</h5>
                </div>
                <div class="card-body">
                    <a href="{{ path('app_game_index') }}" class="btn btn-primary">Gérer mes jeux</a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
